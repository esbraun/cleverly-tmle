# Known-stochastic-regime evidence artifacts

No maintained external implementation is claimed as a comparator for this parameter. The
registered run writes a zero-row equivalence artifact and validates `cleverly` against the known
finite-law truth and independent repeated-sampling properties.

Run `python -m tests.canonical.stochastic_regimes.regenerate` to rebuild the committed artifacts.
